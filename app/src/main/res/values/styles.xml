<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>
    <!-- item name="android:windowBackground"         设置背景透明度及其颜色值 -->
    <!-- item name="android:windowIsTranslucent"      设置当前Activity是否透明-->
    <!-- item name="android:windowAnimationStyle"     设置当前Activity进出方式-->
    <style name="translucent" parent="AppTheme">
        <item name="android:windowBackground">@color/hierachyBgColor</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="FullScreenDialog">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="PrimaryButtonStyle">
        <item name="android:background">@drawable/bg_primary_button</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!--<style name="tabStyle">-->
    <!--<item name="app:tabBackground">@color/colorPrimaryDark</item>-->
    <!--<item name="app:tabIndicatorColor">@android:color/transparent</item>-->
    <!--<item name="app:tabSelectedTextColor">@color/colorAccent</item>-->
    <!--<item name="app:tabTextColor">@android:color/white</item>-->
    <!--</style>-->
</resources>
